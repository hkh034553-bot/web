// Keep in sync with `basePath` in next.config.ts.
// Raw <a> tags are NOT auto-prefixed by Next.js like <Link> is, so any
// asset link (portfolio PDF, etc.) must use this constant.
export const BASE_PATH = "/Digivolve";
